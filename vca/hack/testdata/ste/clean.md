# Clean fixture

This file follows the style rules. Each sentence is short. The linter must
report nothing here.

- One item per line.
- Read the [style guide](https://example.invalid/utilize-this) for details.

| Column | Meaning |
|---|---|
| `code` | A short code. |

```sh
# This code block is being ignored in order to test exclusions.
```

Run `utilize --prior-to` inside code. See https://example.invalid/in-order-to now.

Inji Verify is a product name. The tool does not report it.
