{{- if .Values.issuer.ingress.enabled }}
{{- $host := include "walt-issuer.ingressHost" . }}
{{- if not $host }}
{{- fail "issuer.ingress.host or issuer.baseUrl or global.domain must be set" }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "walt-issuer.fullname" . }}
  labels: {{- include "walt-issuer.labels" . | nindent 4 }}
  annotations:
    {{- /* Merge rather than concatenate. The chart's own values already set
         cert-manager.io/cluster-issuer (default "selfsigned"), so emitting
         the global one as a second line produced a DUPLICATE YAML key --
         invalid YAML that kubeconform rejects and that Kubernetes would
         resolve by silently taking the last value. global.clusterIssuer is
         the deployment-wide choice, so it wins over the chart default. */}}
    {{- $ann := deepCopy (.Values.issuer.ingress.annotations | default dict) }}
    {{- if .Values.global.clusterIssuer }}
    {{- $_ := set $ann "cert-manager.io/cluster-issuer" .Values.global.clusterIssuer }}
    {{- end }}
    {{- toYaml $ann | nindent 4 }}
spec:
  ingressClassName: {{ .Values.issuer.ingress.className }}
  {{- if .Values.issuer.ingress.tls }}
  tls:
    - hosts: [{{ $host | quote }}]
      secretName: {{ include "walt-issuer.fullname" . }}-tls
  {{- end }}
  rules:
    - host: {{ $host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "walt-issuer.fullname" . }}
                port: { name: http }
{{- end }}
