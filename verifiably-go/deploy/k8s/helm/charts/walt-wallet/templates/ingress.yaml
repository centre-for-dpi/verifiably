{{- if .Values.wallet.ingress.enabled }}
{{- $host := include "walt-wallet.ingressHost" . }}
{{- if not $host }}
{{- fail "wallet.ingress.host or wallet.baseUrl or global.domain must be set" }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "walt-wallet.fullname" . }}
  labels: {{- include "walt-wallet.labels" . | nindent 4 }}
  annotations:
    {{- /* Merge rather than concatenate. The chart's own values already set
         cert-manager.io/cluster-issuer (default "selfsigned"), so emitting
         the global one as a second line produced a DUPLICATE YAML key --
         invalid YAML that kubeconform rejects and that Kubernetes would
         resolve by silently taking the last value. global.clusterIssuer is
         the deployment-wide choice, so it wins over the chart default. */}}
    {{- $ann := deepCopy (.Values.wallet.ingress.annotations | default dict) }}
    {{- if .Values.global.clusterIssuer }}
    {{- $_ := set $ann "cert-manager.io/cluster-issuer" .Values.global.clusterIssuer }}
    {{- end }}
    {{- toYaml $ann | nindent 4 }}
spec:
  ingressClassName: {{ .Values.wallet.ingress.className }}
  {{- if .Values.wallet.ingress.tls }}
  tls:
    - hosts: [{{ $host | quote }}]
      secretName: {{ include "walt-wallet.fullname" . }}-tls
  {{- end }}
  rules:
    - host: {{ $host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "walt-wallet.fullname" . }}
                port: { name: http }
{{- end }}
